const compareImages = async (sketchUri, photoUris, { options }) => {
  // options 에서 상위 몇개 추출할건지..  등 전달
  // 1. normalize 호출 :  greyscale
  // 2. dhash : 양 옆 비교해서 2진법 - > 16진수 16개로  변경(비교용이))
};
export default compareImages;
